export class Color {
  id: string;
  color: string;
  color_id: string;

  constructor(id: string, color: string, color_id: string) {
    this.id = id;
    this.color = color;
    this.color_id = color_id;
  }
}
