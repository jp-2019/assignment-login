export class Breed {
  id: string;
  breed: string;
  breed_id: string;

  constructor(id: string, breed: string, breed_id: string) {
    this.id = id;
    this.breed = breed;
    this.breed_id = breed_id;
  }
}
